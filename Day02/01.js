//Create a function that takes two parameters (a number and a string) and returns a string. Add type annotations to the function parameters and return type.
function Sum(a, b) {
    return a.toString() + b;
}
var result = Sum(10, "15");
console.log(result);
